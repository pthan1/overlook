import chai from 'chai';
const expect = chai.expect;

describe('UserModel', function() {
  it('', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });
});
