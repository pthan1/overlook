import chai from 'chai';
const expect = chai.expect;

describe('BookingModel', function() {
  it('should get all bookings data for hotel', function() {
    expect(true).to.equal(true);
  });

  it('should set all bookings data for hotel', function () {
    expect(true).to.equal(true);
  });

  it('should return bookings data for a user', function () {
    
    expect(true).to.equal(true);
  });

  it('should store instances of bookings for user', function () {
    expect(true).to.equal(true);
  });


});
