import chai from 'chai';
const expect = chai.expect;

describe('BookingController', function() {
  it('should receive bookings data from model', function() {
    
  });

  it('should create instances of bookins for model', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });

  it('', function () {
    expect(true).to.equal(true);
  });


});
